---
slug: /damai/architecture-components/grey-release
title: "设计灰度环境服务调用：灰度调用组件、灰度路由、LoadBalancer扩展、服务实例过滤详解"
sidebar_label: "设计灰度环境服务调用"
pagination_label: "设计灰度环境服务调用"
description: "围绕《设计灰度环境服务调用》，重点讲解灰度调用组件、灰度路由、LoadBalancer扩展、服务实例过滤、灰度标识透传与Gateway集成等技术实现与工程落地细节。内容进一步围绕注册中心联动、灰度标签路由等关键主题展开。通过原理拆解、实现步骤与适用场景说明相关方案如何落地。同时补充常见问题、排查思路、项目实践建议与技…"
keywords: ["灰度调用组件", "灰度路由", "LoadBalancer扩展", "服务实例过滤", "灰度标识透传", "Gateway集成", "注册中心联动", "灰度标签路由"]
---

# 设计灰度环境服务调用

import VipInline from '@site/src/components/VipInline';


## 灰度调用组件
[技术精华-给你讲透为什么需要灰度环境](/damai/tech-highlights/why-grey-release) 的文章中，介绍了什么是灰度环境，以及灰度和生产环境共用同一个注册中心，服务是如何调用。



在本文中，就来介绍灰度调用组件是如何实现灰度和生产环境隔离和调用的



## 使用
### gateway依赖


```xml
<dependency>
    <groupId>com.example</groupId>
    <artifactId>damai-service-gray-transition-gateway-framework</artifactId>
    <version>${revision}</version>
</dependency>
```



### web服务依赖
```xml
<dependency>
    <groupId>com.example</groupId>
    <artifactId>damai-service-gray-transition-webmvc-framework</artifactId>
    <version>${revision}</version>
</dependency>
```



### 配置
```yaml
spring:
  cloud:
    nacos:
      discovery:
        metadata:
          gray: true
```



### 请求
+ 如果是灰度环境，则在请求头添加`gray=true`即可
+ 如果是生产环境，则无需配置



## 介绍


### 请求的流程
- 灰度环境中的请求在开始时，在请求头中添加一个标识`gray=true`，然后开发一个过滤ribbon负载均衡服务的过滤器，在这里进行过滤服务
- 如果请求标识含有`gray=true`，那么就是来自灰度的请求，那么就调用元数据配置了`gray=true`的服务，如果没有标识`gray=true`的服务，那么就是说明没有灰度只有生产服务，那么就直接调用生产的服务
- 如果请求的请求头中没有标识`gray=true`，那么就是来自生产的请求，那么就必须调用没有标识`gray=true`也就是只能调用生产环境的服务

<VipInline />