---
slug: /damai/tech-highlights/thread-exception-handling
title: "完全解读线程和线程池异常处理原理：线程异常处理、UncaughtExceptionHandler详解"
sidebar_label: "完全解读线程和线程池异常处理原理"
pagination_label: "完全解读线程和线程池异常处理原理"
description: "围绕《完全解读线程和线程池异常处理原理》，重点讲解UncaughtExceptionHandler、execute与submit差异、FutureTask#setException、runWorker 与线程池死锁排查等技术实现与源码细节。内容进一步围绕线程异常处理、setDefaultUncaughtExcepti…"
keywords: ["线程异常处理", "UncaughtExceptionHandler", "setDefaultUncaughtExceptionHandler", "ThreadPoolExecutor", "execute与submit差异", "FutureTask#setException", "runWorker", "线程池死锁"]
---

# 完全解读线程和线程池异常处理原理

import VipInline from '@site/src/components/VipInline';

## 1 线程的异常处理


**案例:**

```java
public static void testThreadException(){
        try {
            new Thread(() -> {
                System.out.println("===线程执行===");
                throw new RuntimeException("出现异常");
            }).start();
        }catch (Exception e) {
            System.out.println("异常:"+e.getMessage());
        }
}
```



**结果:**



```java
===线程执行===
Exception in thread "Thread-1" java.lang.RuntimeException: 出现异常
    at test.SpringbootApplication.lambda$testThreadException$0(SpringbootApplication.java:53)
    at java.lang.Thread.run(Thread.java:748)
```



可以发现线程本身可以因为出现异常而终止，但是使用try catch进行处理多线程异常是没有作用的



## 2 UncaughtExceptionHandle


**Thread.UncaughtExceptionHandler 这是Thread类中的接口**



**官方文档中的描述:**

- 当线程由于未捕获的异常突然终止时调用的处理程序接口。
- 当线程由于未捕获的异常而即将终止时，Java 虚拟机将使用`getUncaughtExceptionHandler`查询线程的`UncaughtExceptionHandler`并将调用处理程序的`uncaughtException`方法，将线程和异常作为参数传递。
- 如果一个线程没有明确设置它的`UncaughtExceptionHandler` ，那么它的`ThreadGroup`对象作为它的`UncaughtExceptionHandler`。 如果`ThreadGroup`对象对处理异常没有特殊要求，它可以将调用转发给默认的未捕获异常处理程序。

<VipInline />