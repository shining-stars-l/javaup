---
slug: /damai/interview-highlights/multi-cache-consistency
description: "围绕《如何确保多级缓存的一致性》，重点讲解RedisStream消息同步、多实例缓存一致性、缓存失效通知、Redis缓存删除与Elasticsearch数据删除联动机制。"
keywords: ["多级缓存一致性", "RedisStream", "多实例缓存同步", "缓存失效通知", "消息监听", "Redis缓存删除", "Elasticsearch数据删除", "最终一致性"]
---

# 如何确保多级缓存的一致性

import VipInline from '@site/src/components/VipInline';

在讲解多级缓存章节中，我们介绍了如何引入本地缓存，以及和Redis缓存进行配合，建议没看过的小伙伴，先去学习这一章节

[Redis性能真的足够吗？百万并发的终极杀招 "多级缓存"](/damai/interview-highlights/redis-performance-multi-cache)


而本文的重点是来介绍如果有修改的操作，那该如何去解决缓存一致性呢？又比如缓存中数据都是根据节目演出时间来设置的，如果遇到了突发情况，要求节目提前下线，要如何在缓存中提前过期呢？

## 思考
对于这种一致性问题，可以使用通用的方案，也就是当修改数据库中的数据后，将对应的缓存清空，Redis的缓存好办，可以直接删除掉

### 但是本地缓存就会有个问题，如果存在多实例，那么要怎么处理？
就拿节目服务来说，假设线上部署了5个实例节点，经过一段时间运行后，每个实例都有了自己的本地缓存，那么如果进行了数据的修改操作后，就要将这5个实例节点的数据都清空

那么要如何通知这5个节点呢？可以有这几种方式：

<VipInline />