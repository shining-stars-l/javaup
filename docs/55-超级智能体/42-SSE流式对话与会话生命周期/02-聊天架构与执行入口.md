---
slug: /super-agent/chat-stream-lifecycle/open-conversation-stream-execution-entry
title: "聊天架构与执行入口：openConversationStream、Flux.defer详解"
sidebar_label: "聊天架构与执行入口"
pagination_label: "聊天架构与执行入口"
description: 总结 BusinessChatService.openConversationStream 的后端聊天架构，从 Flux.defer、会话租约、轮次引导、SSE通道绑定，到 buildConversationExecution 准备执行计划并路由到 ConversationExecutor 的完整入口流程。
keywords: [openConversationStream, buildConversationExecution, Flux.defer, doOnSubscribe, 会话租约, TaskInfo, ConversationExecutor, SSE生命周期, Super Agent]
---

import VipInline from '@site/src/components/VipInline';

# 聊天架构与执行入口

前面两篇文档其实已经把聊天架构讲得比较完整了：

- [从用户提问到答案返回的总流程](/super-agent/chat-system-architecture/end-to-end-flow)：讲的是一条用户问题从入口到答案返回的全局链路。
- [stream接口与SSE事件协议](/super-agent/sse-chat-lifecycle/stream-api-and-sse-protocol)：讲的是 SSE 事件协议、Sink、安全推送和流关闭。

但它们中间还差一个很关键的“收束点”：`BusinessChatService#openConversationStream` 到底是怎么把一次请求启动起来，又是怎么走到 `ConversationExecutor` 入口的？

这篇来专门讲解从请求启动一直到具体执行器的选择。而具体执行器内部逻辑而在后续的章节详细讲解

:::info 阅读边界
这里会提到 `prepareExecutionPlan(taskInfo)`，因为它是 `buildConversationExecution` 里面生成执行计划的关键步骤。但它内部的问题改写、路由识别、历史压缩、检索规划等细节先不展开。
:::

## 后端主流程总图

```plantuml title="从对话到执行器入口总览" width="70%" align="left"
@startuml
skinparam backgroundColor #F8FAFC
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 13
skinparam shadowing false
skinparam roundCorner 18
skinparam ArrowColor #2563EB
skinparam ArrowThickness 1.6

skinparam activity {
  BackgroundColor #FFFFFF
  BorderColor #94A3B8
  FontColor #1E293B
  FontSize 13
  FontStyle plain
  MinimumWidth 420
  BarColor #2563EB
  DiamondBackgroundColor #FFF7ED
  DiamondBorderColor #FB923C
  StartColor #2563EB
  EndColor #2563EB
}

skinparam note {
  BackgroundColor #EFF6FF
  BorderColor #93C5FD
  FontColor #1E293B
}

start

:入口与延迟启动
openConversationStream(request)
Flux.defer(() -> openDeferredConversationStream(request))
第一层延迟：只有订阅时才真正启动会话;

:启动保护
buildLaunchPlan(request)
claimConversationLease(launchPlan)
先整理请求，再抢占 Redis 会话租约;

if (Redis 租约拿到了？) then (否)
  :拒绝响应
  rejectionFlux(error)
  只返回一条 error 事件，执行链不再往下走;
  stop
else (是)
endif

:轮次引导
conversationArchiveStore.startExchange(...)
createTaskInfo(launchPlan, exchangeView)
chatRuntimeRegistry.register(taskInfo)
创建轮次、构建 TaskInfo、注册本地运行态;

if (本地运行态注册成功？) then (否)
  :引导失败收尾
  failBootstrappedExchange(...)
  releaseLeaseQuietly(...)
  rejectionFlux(error)
  已创建的轮次补成失败态，租约也要释放;
  stop
else (是)
endif

:SSE 通道绑定
bindClientChannel(taskInfo)
sink.asFlux()
doOnSubscribe -> activateGeneration(taskInfo)
第二层延迟：通道建好后，等订阅发生再启动生成;

:后台生成激活
startLeaseRenewal(taskInfo)
buildConversationExecution(taskInfo).subscribe()
先启动租约续期，再订阅真正的执行流;

:执行器入口
Flux.defer(...)
safeEmit(thinking)
prepareExecutionPlan(taskInfo)
conversationExecutorRegistry.get(plan.getMode())
executor.execute(taskInfo)
到这里才进入具体执行器;

:统一接管输出
doOnNext -> emitModelChunk(...)
执行器只产出文本 chunk，SSE 推送和答案累计由外层统一处理;

if (执行流结束方式) then (正常完成)
  :成功收尾
  doOnComplete -> finishSuccessfully(...)
  补发引用、推荐追问，完成归档;
else (异常)
  :失败收尾
  doOnError -> finishWithFailure(...)
  写失败状态，关闭流并保留错误信息;
endif

:统一资源清理
cleanup(taskInfo)
停止续租和执行流
releaseLeaseQuietly(...)
移除 chatRuntimeRegistry;

stop

@enduml
```

这张图最重要的是要知道这三件事情：

1. **启动不是立刻发生的**：`Flux.defer` 和 `.doOnSubscribe` 把真正的会话启动动作延后到订阅发生时。
2. **执行前有两道互斥保护**：Redis 分布式租约防止跨实例并发，本地运行态注册表防止当前实例内重复执行。
3. **`buildConversationExecution` 是分水岭**：它前面是会话生命周期管理，它里面开始准备执行计划，再把任务交给具体执行器。

## 第一层延迟：openConversationStream

<VipInline />
