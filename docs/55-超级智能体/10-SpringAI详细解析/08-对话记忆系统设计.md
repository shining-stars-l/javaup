---
slug: /super-agent/spring-ai-detail/chat-memory
description: "系统讲解AI对话记忆的设计与实现：短期记忆与长期记忆的区别、Spring AI的ChatMemory体系、基于MySQL的持久化存储，以及记忆管理的最佳实践"
keywords: ["对话记忆", "ChatMemory", "短期记忆", "长期记忆", "MessageWindowChatMemory", "持久化记忆", "MySQL"]
---

import VipInline from '@site/src/components/VipInline';

# 对话记忆系统设计

和ChatGPT聊过天的人都知道，它能记住你之前说的话。问它"我刚才说了什么"，它能准确回答。这种能力叫做**对话记忆**。

对于AI应用来说，记忆能力至关重要。没有记忆，每次对话都是"新朋友"，用户体验会很差。今天我们来深入聊聊Spring AI中对话记忆的设计与实现。

## 为什么需要对话记忆

先来理解一个基本事实：**大模型本身是无状态的**。

每次你调用大模型API，对它来说都是一次全新的请求。它不知道一秒钟前你问过什么，更不知道昨天聊了什么话题。

那ChatGPT是怎么做到"记住"对话的？答案是：**把历史对话一起发给模型**。

```plantuml title="没有记忆与带记忆对话的差异" width="45%" align="left"
@startuml
skinparam backgroundColor #F8FAFC
skinparam roundcorner 18
skinparam shadowing false
skinparam defaultFontName Microsoft YaHei
skinparam defaultFontSize 14
skinparam defaultTextAlignment center
skinparam linetype ortho
skinparam packageStyle rectangle
skinparam ArrowColor #2563EB
skinparam ArrowThickness 1.4
skinparam ArrowFontColor #1D4ED8
skinparam ArrowFontSize 13
skinparam sameClassWidth true
skinparam classAttributeIconSize 0
hide empty members
hide circle
hide stereotype

skinparam package {
  BackgroundColor #FFFFFF
  BorderColor #BFDBFE
  FontColor #0F172A
}

skinparam class {
  BackgroundColor #FFFFFF
  BorderColor #BFDBFE
}

package "没有记忆" as NoMemory {
  class "历史消息\n未被保留" as NM_H1 #F8FAFC
  class "请求中的历史上下文\n为空" as NM_H2 #FFF7ED
  class "当前问题\n用户：我叫什么？" as NM_Q #EFF6FF
  class "上下文不足\n无法关联姓名" as NM_CTX #FFF7ED
}

package "带记忆" as WithMemory {
  class "历史消息\n用户：我叫张三" as WM_H1 #EFF6FF
  class "历史回复\nAI：你好，张三！" as WM_H2 #ECFEFF
  class "当前问题\n用户：我叫什么？" as WM_Q #EFF6FF
  class "完整上下文\n一起发送给模型" as WM_CTX #F0F9FF
}

class "AI：我无法确定你的名字" as NM_A #FFF7ED
class "AI：你叫张三" as WM_A #ECFDF5

NM_H1 -[hidden]right- WM_H1
NM_H2 -[hidden]right- WM_H2
NM_Q -[hidden]right- WM_Q
NM_CTX -[hidden]right- WM_CTX
NM_A -[hidden]right- WM_A

NM_H1 -down-> NM_H2
NM_H2 -down-> NM_Q
NM_Q -down-> NM_CTX
NM_CTX -down-> NM_A

WM_H1 -down-> WM_H2
WM_H2 -down-> WM_Q
WM_Q -down-> WM_CTX
WM_CTX -down-> WM_A
@enduml
```

每次对话时，把之前的所有消息都带上，模型就能"理解"上下文。这就是对话记忆的本质。

:::info 对话记忆的本质
大模型本身是**无状态的**，每次 API 调用对它来说都是全新请求。实现"记忆"的方式是：把历史对话消息一起发给模型，模型从完整的上下文中"理解"之前说了什么。
:::

<VipInline />
