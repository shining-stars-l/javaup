---
slug: /super-agent/react-agent-executor/interceptor-chain
title: "拦截器链的源码解析：DashScopeCompatibilityInterceptor详解"
sidebar_label: "拦截器链的源码解析"
pagination_label: "拦截器链的源码解析"
description: "深入讲解 ReactAgent 拦截器链中两个自定义拦截器的源码实现：DashScopeCompatibilityInterceptor 处理 API 兼容性问题，TavilyToolInputFallbackInterceptor 保障工具入参的有效性。内容进一步围绕ModelInterceptor、ToolInt…"
keywords: [拦截器链, DashScopeCompatibilityInterceptor, TavilyToolInputFallbackInterceptor, ModelInterceptor, ToolInterceptor, DashScope兼容, 工具入参兜底, 碎片化工具调用]
---

import VipInline from '@site/src/components/VipInline';

# 拦截器链的源码解析

## 拦截器在 Agent 中的位置

在前面的 Bean 配置文档中，我们看到 ReactAgent 注册了四个拦截器。其中 `ToolRetryInterceptor` 和 `ToolErrorInterceptor` 是框架内置的，配置一下参数就行。但另外两个是项目自己实现的，它们解决的是实际对接过程中遇到的真实问题。

这篇我们来看这两个自定义拦截器的源码。

## DashScopeCompatibilityInterceptor：API 兼容拦截器

### 它解决什么问题？

项目使用的是阿里云 DashScope 的大模型 API（通过 OpenAI 兼容接口调用）。但 DashScope 和标准 OpenAI API 之间有几个不兼容的地方：

1. **不支持 `parallelToolCalls` 参数**——传了会报错
2. **不支持 `streamOptions` 参数**——传了会报错
3. **工具调用可能被拆成碎片返回**——一个完整的工具调用被拆成多个片段，需要合并

这个拦截器就是在模型调用前后做兼容处理，让上层代码不需要关心这些差异。

<VipInline />