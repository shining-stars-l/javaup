---
slug: /super-agent/feature-guide/conversation-memory
description: 会话记忆管理的功能详解，包括三种记忆策略、LLM摘要压缩、增量合并、滑动窗口等机制。
keywords: [会话记忆, 摘要压缩, 滑动窗口, PersistentConversationMemoryService, 记忆策略, 增量合并]
---

import VipInline from '@site/src/components/VipInline';

# 会话记忆管理

多轮对话中，系统需要"记住"之前聊过什么。但历史对话越来越长，不可能全部塞给模型。这个模块解决的就是这个问题：怎么在有限的 token 预算内，保留最有价值的历史信息。

## 三种记忆策略

项目支持三种策略，复杂度递增：

| 策略 | 说明 | 适用场景 |
|------|------|---------|
| 无记忆 | 每次对话都是独立的，不带任何历史 | 单次问答、不需要上下文 |
| 滑动窗口 | 保留最近 N 轮的原始对话文本 | 短对话、上下文简单 |
| 摘要压缩 | 长期 LLM 摘要 + 最近几轮原文 | 长对话、需要记住早期信息 |

第三种是项目的默认策略，也是最复杂的。

## 核心接口

### ConversationMemoryService

<VipInline />