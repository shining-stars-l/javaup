---
slug: /super-agent/chat-executors/vector-retrieval-channel
description: 深入讲解 VectorRetrievalChannel.retrieve 和 DocumentKnowledgeServiceImpl.vectorSearch 的完整流程，包括向量生成、pgvector SQL 构建、结果转换等核心步骤。
keywords: [VectorRetrievalChannel, vectorSearch, pgvector, embedding, 向量检索, similarity_score, appendSectionFilters, buildRetrievedDocument]
---

import VipInline from '@site/src/components/VipInline';

# 向量检索通道的执行流程

上一篇讲了检索请求是怎么构建的。这篇来看向量检索通道拿到这个请求之后，具体是怎么执行的。

## VectorRetrievalChannel.retrieve 源码

```java
@Override
public RetrievalChannelResult retrieve(String subQuestion, ConversationExecutionPlan plan) {
    // 调用 DocumentKnowledgeService 的向量检索方法，topK 从配置中读取。
    List<Document> documentList = documentKnowledgeService.vectorSearch(
        documentRetrieveRequestFactory.build(subQuestion, plan, properties.getVectorTopK())
    );
    // 返回通道结果，包含通道名称和检索到的文档列表。
    return new RetrievalChannelResult(channelName(), documentList);
}
```

方法很简洁，就两步：先用工厂构建请求（上一篇已经讲过），再调 `vectorSearch` 执行检索。核心逻辑都在 `vectorSearch` 里。

## vectorSearch 方法完整流程

整体流程用图表示：

```plantuml title="向量检索流程图" width="55%" align="left"
@startuml
start
:接收 DocumentRetrieveRequest;
if (isSearchableRequest?) then (否)
  :返回空列表;
  stop
endif
:requireEmbeddingModel() 获取嵌入模型;
:embeddingModel.embed(retrievalQuery)\n生成问题向量;
:toVectorLiteral 转成 pgvector 字面量\n格式：[0.1, 0.2, ...];
:listDescriptorMap 加载文档描述信息;
:resolveMetadataScope 解析过滤范围;
if (documentIds 或 taskIds 为空?) then (是)
  :返回空列表;
  stop
endif
:格式化 VECTOR_RETRIEVE_SQL_TEMPLATE;
:appendSectionFilters 追加可选过滤条件;
:拼接 ORDER BY + LIMIT;
:组装参数列表\n[向量, docIds, taskIds, 过滤参数, 向量, topK];
:pgVectorJdbcTemplate.query 执行查询;
:每行结果调用 buildRetrievedDocument\n转成 Spring AI Document;
:返回 Document 列表;
stop
@enduml
```

<VipInline />