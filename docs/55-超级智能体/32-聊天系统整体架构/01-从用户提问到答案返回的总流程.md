---
slug: /super-agent/chat-system-architecture/end-to-end-flow
description: 从用户在前端输入问题开始，到后端接收请求、抢占租约、构建执行计划、选择执行器、流式输出答案、收尾落库的完整链路拆解，配合核心源码逐段讲解每一步到底在干什么。
keywords: [聊天流程, SSE流式, 会话租约, 执行计划, 执行器路由, TaskInfo, BusinessChatService, 对话链路]
---

import VipInline from '@site/src/components/VipInline';

# 从用户提问到答案返回的总流程

这篇文档会把 Super Agent 聊天系统后端的完整链路拆开来讲，从用户点击"发送"的那一刻开始，一直到答案流式输出到前端、最后落库收尾为止。每个关键步骤都会贴出对应的源码，加上注释说明它在整条链路里的作用。

看完这篇，你会对"一个问题是怎么从 Controller 一路走到模型输出再回到前端"有一个完整的认知。

## 总流程概览

先看一张全局流程图，对整条链路有个直观印象：

```plantuml title="流程图"
@startuml
skinparam backgroundColor #FAFBFC
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 13
skinparam shadowing false
skinparam roundCorner 12
skinparam ArrowColor #5B8DEF
skinparam ArrowThickness 1.5

skinparam activity {
  BackgroundColor #FFFFFF
  BorderColor #D0D7DE
  FontColor #1F2328
  DiamondBackgroundColor #FFF8E1
  DiamondBorderColor #F9A825
}

|#EBF3FF|前端|
start
:用户输入问题，点击发送;
:POST /api/chat/stream\n携带 question、conversationId、chatMode;

|#F0FFF0|Controller 层|
:BusinessChatController.stream()\n接收请求，参数校验;

|#FFF8E1|Service 层 - 启动阶段|
:openConversationStream()\n使用 Flux.defer 延迟启动;
:buildLaunchPlan()\n规范化参数，构建启动计划;
:claimConversationLease()\n抢占 Redis 分布式租约;

if (租约抢占成功？) then (失败)
  :返回拒绝流\n"该会话当前正在执行中";
  stop
else (成功)
endif

:bootstrapConversation()\n创建轮次记录、构建 TaskInfo、注册运行态;
:bindClientChannel()\n绑定 SSE 输出通道;

|#F3E5F5|Service 层 - 执行阶段|
:activateGeneration()\n启动租约续期 + 执行链路;
:buildConversationExecution()\n组装完整执行流;
:prepareExecutionPlan()\n编排器分析意图，生成执行计划;
:ConversationExecutorRegistry.get(mode)\n根据执行模式选择执行器;
:executor.execute(taskInfo)\n执行器开始工作（RAG/Agent/澄清等）;

|#E8F5E9|Service 层 - 输出阶段|
:emitModelChunk()\n逐块推送文本到前端;
:finishSuccessfully()\n补发引用、推荐追问;
:落库归档 + 刷新摘要 + 释放资源;

|#EBF3FF|前端|
:前端逐段接收 SSE 事件\n实时渲染答案;
stop

@enduml
```

接下来我们按照这张图的顺序，逐步拆解每个阶段的源码。

## 入口：Controller 接收请求

一切从前端的 POST 请求开始。前端把用户的问题、会话 ID、聊天模式等信息打包成 `ChatRequestDto`，发到 `/api/chat/stream` 接口。

<VipInline />
