---
slug: /super-agent/chat-system-architecture/frontend-backend-modules-and-call-chain
description: 深入拆解 Super Agent 后端的 Maven 模块划分、聊天业务包结构、各层核心类的职责，以及它们之间的调用关系，帮你建立对整个后端架构的全局认知。
keywords: [后端模块, 包结构, 调用关系, Maven 模块, 分层架构, 聊天服务, RAG, 执行器, 文档管理]
---

import VipInline from '@site/src/components/VipInline';

# 前后端模块划分与调用关系

这篇文档聚焦后端部分，把 Super Agent 的 Maven 模块划分、聊天业务的包结构、各层核心类的职责和调用关系都理清楚。看完之后，你拿到任何一个类名，都能快速定位它在整个架构中的位置和作用。

## Maven 模块总览

Super Agent 后端采用多模块 Maven 结构，每个模块有明确的职责边界：

```plantuml
@startuml
skinparam backgroundColor #FAFBFC
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 13
skinparam shadowing false
skinparam roundCorner 10
skinparam packageStyle rectangle
skinparam ArrowColor #5B8DEF
skinparam ArrowThickness 1.5

skinparam package {
  BackgroundColor #FFFFFF
  BorderColor #D0D7DE
  FontColor #1F2328
}

skinparam component {
  BackgroundColor #EBF3FF
  BorderColor #5B8DEF
  FontColor #1F2328
}

package "super-agent (父工程)" {

  package "super-agent-common" as common {
    component "common-frame" as cf
    component "common-web" as cw
    note bottom of cf
      枚举、工具类、通用响应
      ChatQueryMode、ChatTurnStatus 等
    end note
  }

  package "super-agent-id-generator-framework" as idgen {
    component "ID 生成器" as ig
    note bottom of ig
      分布式 ID 生成
    end note
  }

  package "super-agent-redisson-framework" as redis {
    component "分布式锁" as rl
    component "重复执行限制" as rel
    component "Redis 租约" as lease
    note bottom of lease
      RedisLeaseManager
      会话并发控制的基础
    end note
  }

  package "super-agent-business" as biz {
    component "super-agent-business-chat" as chat
    note bottom of chat
      聊天系统的全部业务代码
      Controller / Service / RAG / 文档管理
    end note
  }
}

chat --> cf : 使用枚举和工具类
chat --> cw : 使用 Web 基础设施
chat --> ig : 生成会话/轮次 ID
chat --> lease : 会话分布式租约

@enduml
```

| 模块 | 职责 |
| :--- | :--- |
| `super-agent-common` | 通用基础设施：枚举定义（`ChatQueryMode`、`ChatTurnStatus`）、工具类、统一响应封装 |
| `super-agent-id-generator-framework` | 分布式 ID 生成，为会话和轮次提供全局唯一标识 |
| `super-agent-redisson-framework` | Redis 相关能力：分布式锁、重复执行限制、**会话租约管理**（`RedisLeaseManager`） |
| `super-agent-business-chat` | 聊天系统的全部业务代码，是最核心的模块 |

## 聊天业务包结构

`super-agent-business-chat` 是整个聊天系统的核心，内部按职责划分成多个包。下面这张图展示了包之间的层次关系：

```plantuml
@startuml
skinparam backgroundColor #FAFBFC
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 13
skinparam shadowing false
skinparam roundCorner 8
skinparam ArrowColor #5B8DEF
skinparam ArrowThickness 1.5
skinparam linetype ortho
skinparam nodesep 40
skinparam ranksep 50

skinparam rectangle {
  FontColor #1F2328
  BorderColor #D0D7DE
  StereotypeFontSize 0
}

' ── Row 1 ──
rectangle "controller" as ctrl #EBF3FF
rectangle "dto / vo" as dtovo #F0FFF0

' ── Row 2 ──
rectangle "service" as svc #FFF8E1

' ── Row 3 ──
rectangle "support" as sup #E8F5E9
rectangle "data / mapper" as dal #F5F5F5
rectangle "executor" as exec #F3E5F5

' ── Row 4 ──
rectangle "service (rag)" as ragsvc #F3E5F5
rectangle "tool" as tool #FCE4EC
rectangle "manage (文档管理)" as manage #E0F7FA

' ── Row 5 ──
rectangle "retrieve" as ret #F3E5F5

' ── 同行对齐 ──
ctrl -[hidden]r- dtovo
sup -[hidden]r- dal
dal -[hidden]r- exec
ragsvc -[hidden]r- tool
tool -[hidden]r- manage

' ── 纵向骨架 ──
ctrl -[hidden]d- svc
svc -[hidden]d- dal
exec -[hidden]d- ragsvc
ragsvc -[hidden]d- ret

' ── 可见连线 ──
ctrl -d-> dtovo : 接收/返回
ctrl -d-> svc : 调用
svc -d-> sup : SSE 事件
svc -d-> dal : 归档落库
svc -d-> exec : 编排执行
exec -d-> ragsvc : 检索/生成
exec -d-> tool : 工具调用
ragsvc -d-> ret : 多路检索
ragsvc -r..> manage : 知识路由/文档查询

@enduml
```

### 各包职责详解

下面逐个说明每个包里装了什么、负责什么：

**controller —— 请求入口**

只有一个类 `BusinessChatController`，负责接收前端 HTTP 请求、参数校验，然后转交给 Service 层。这一层不做任何业务逻辑。

```java
// 控制器只做两件事：接参数、转交 Service
@RestController
@RequestMapping("/api/chat")
public class BusinessChatController {
    private final BusinessChatService businessChatService;

    @PostMapping(value = "/stream", produces = "text/event-stream;charset=UTF-8")
    public Flux<String> stream(@Valid @RequestBody ChatRequestDto dto) {
        return businessChatService.openConversationStream(dto);
    }
    // 还有 stop、reset、session 等接口...
}
```

**dto / vo —— 数据传输**

- `dto` 包：前端传入的请求参数，比如 `ChatRequestDto`（问题、会话 ID、聊天模式）
- `vo` 包：返回给前端的响应结果，比如 `ConversationStopVo`、`ConversationResetVo`

**service —— 业务核心**

这是整个聊天系统最重要的包

<VipInline />
