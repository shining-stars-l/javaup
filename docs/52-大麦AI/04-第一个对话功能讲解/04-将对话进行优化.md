---
slug: /damai-ai/first-chat/optimize-conversation
description: "AI对话优化实践，围绕会话记忆、检索增强、提示词约束和可观测性追踪进行全链路调优，提升多轮问答稳定性与结果准确度。"
keywords: ["对话优化", "会话记忆窗口", "RAG增强", "提示词约束", "检索参数调优", "Token成本控制", "响应稳定性", "多轮问答"]
---

# 将对话进行优化

import VipInline from '@site/src/components/VipInline';

## 项目已有的优化点
本项目不是“裸调用模型”，而是通过 Advisor 和检索策略做多层优化：

- 记忆优化：`MessageChatMemoryAdvisor` 维持最近窗口记忆。
- 标题优化：`ChatTypeTitleAdvisor` 自动生成会话标题，提升历史可读性。
- 追踪优化：`AiObservabilityAdvisor` 记录 token、耗时、费用。
- 检索优化（RAG v2）：`HybridSearchService` 使用“向量 + 关键词 + RRF + rerank”。

## 优化顺序建议
1. 先保证 system prompt 稳定（角色与边界清晰）。
2. 再加会话记忆（避免“失忆”）。
3. 再加检索增强（解决“编造”）。
4. 最后做观测与成本治理。


<VipInline />
