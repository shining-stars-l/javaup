---
slug: /damai-ai/conversation-list/ai-summarize-title
---

# 利用AI总结标题

import VipInline from '@site/src/components/VipInline';

当点击 新的聊天 按钮后，新出现的会话聊天标题是 “新的对话”

<!-- 这是一张图片，ocr 内容为：大麦AI 麦小蜜 聊天记录 新的聊天 大麦系统智能客服 新的对话 请描述您的问题,麦小蜜会帮您解答哦... -->
![](https://cdn.nlark.com/yuque/0/2025/png/22643320/1750149163750-7618b08a-9df2-431f-8525-1b28c80abaa0.png)



当和 ai 进行具体的对话后，能够根据对话的内容来总结出一个标题，自动的更新到对应的会话聊天列表上，把 “新的对话” 标题更新为更有实际意义的标题

<!-- 这是一张图片，ocr 内容为：大麦AI 麦小蜜 聊天记录 新的聊天 大麦系统智能客服 咨询周杰伦演唱会信息 想咨询周杰伦的演唱会 您好!麦小蜜很高兴为您服务!关于周杰伦的淡唱会,为了更好地为您查询相关信息,请问您是否有以下信息可以提供呢? 1.演唱会的演出地点(城市名,例如:北京) 2.演唱会的时间 如果有其中任何一项信息,都可以帮助我更准确地为您查询哦!如果没有具体信息,我也可以为您查询近期周杰他的演唱会安排. 请描述您的问题,麦小蜜会帮您解答哦... -->
![](https://cdn.nlark.com/yuque/0/2025/png/22643320/1750149264048-38bbad6e-f505-4074-bfe4-8afb5d8cfc41.png)



SpringAI并没有提供此功能，所以需要我们自己来实现。

## 更新标题
还记得当时在设计会话类型历史表的时候，有个 title 字段

```sql
CREATE TABLE `d_chat_type_history` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键id',
  `type` int NOT NULL COMMENT '会话类型，详见ChatType枚举',
  `chat_id` varchar(225) NOT NULL COMMENT '会话id',
  `title` varchar(512) DEFAULT NULL COMMENT '标题',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `edit_time` datetime DEFAULT NULL COMMENT '编辑时间',
  `status` tinyint(1) DEFAULT '1' COMMENT '1:正常 0:删除',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=65 DEFAULT CHARSET=utf8mb3 COMMENT='会话历史表';
```

这个 title 字段就是来实现显示实际内容标题的

## 分析
我们要思考，什么时候这个标题要更新呢？应该是在和 ai 对话后，产生了具体的内容后，根据这段内容来总结出一个标题。

那么得到一个结论，**更新标题的时机应该是在和 ai 对话后，产生了具体的内容后。**

****

还差一个问题，那就是怎么根据这段对话内容来总结出有意义的标题呢？很简单，还是让ai做总结啊！



通过之前的讲解，我们知道了 SpringAI 自动就可以将会话的内容保存下来，项目中使用的数据库来保存。

在创建 ChatClient 时候，添加个 MessageChatMemoryAdvisor 就可以实现了

```java
@Bean
public ChatClient assistantChatClient(DeepSeekChatModel model, ChatMemory chatMemory, AiProgram aiProgram,
                                      ChatTypeHistoryService chatTypeHistoryService,@Qualifier("titleChatClient")ChatClient titleChatClient) {
    return ChatClient
            .builder(model)
            .defaultSystem(DaMaiConstant.DA_MAI_SYSTEM_PROMPT)
            .defaultAdvisors(
                    new SimpleLoggerAdvisor(),
                    ChatTypeHistoryAdvisor.builder(chatTypeHistoryService).type(ChatType.ASSISTANT.getCode()).order(CHAT_TYPE_HISTORY_ADVISOR_ORDER).build(),
                    MessageChatMemoryAdvisor.builder(chatMemory).order(MESSAGE_CHAT_MEMORY_ADVISOR_ORDER).build()
            )
            .defaultTools(aiProgram)
            .build();
}
```



MessageChatMemoryAdvisor 是 SpringAI 已经提供的 advisor，它是在 after 方法中将会话内容进行保存

```java
@Override
public ChatClientResponse after(ChatClientResponse chatClientResponse, AdvisorChain advisorChain) {
    List<Message> assistantMessages = new ArrayList<>();
    if (chatClientResponse.chatResponse() != null) {
        assistantMessages = chatClientResponse.chatResponse()
            .getResults()
            .stream()
            .map(g -> (Message) g.getOutput())
            .toList();
    }
    this.chatMemory.add(this.getConversationId(chatClientResponse.context(), this.defaultConversationId),
            assistantMessages);
    return chatClientResponse;
}
```



**所以根据以上的分析后，实现总结出标题的执行时机要在 MessageChatMemoryAdvisor 的 after 方法之后执行，因为这样才能拿到，用户问ai，ai再回复，这一整个完整来回的对话内容。**



对于这样的要求，答案显而易见还是要自定义 advisor

<VipInline />