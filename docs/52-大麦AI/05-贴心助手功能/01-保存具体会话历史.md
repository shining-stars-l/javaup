---
slug: /damai-ai/assistant/save-conversation-history
title: "保存具体会话历史：会话历史保存、chatId、多轮对话、上下文关联、ChatMemory详解"
sidebar_label: "保存具体会话历史"
pagination_label: "保存具体会话历史"
description: "会话历史保存实现详解，讲解chatId在多轮对话中的上下文关联方式，以及ChatMemory与接口参数协同实现连续问答的核心机制。内容进一步围绕会话窗口、历史回放、接口参数传递等关键主题展开。通过原理拆解、实现步骤与适用场景说明相关方案如何落地。同时补充常见问题、排查思路、项目实践建议与技术面试要点。"
keywords: ["会话历史保存", "chatId", "多轮对话", "上下文关联", "ChatMemory", "会话窗口", "历史回放", "接口参数传递"]
---

# 保存具体会话历史

import VipInline from '@site/src/components/VipInline';

## 创建ChatClient
在实现此功能的第一个步骤就是要把ChatClient创建出来

org.javaup.ai.config.DaMaiAiAutoConfiguration

```java
@Bean
public ChatClient assistantChatClient(DeepSeekChatModel model) {
    return ChatClient
            .builder(model)
            .defaultSystem(DaMaiConstant.DA_MAI_SYSTEM_PROMPT)
            .defaultAdvisors(
                    new SimpleLoggerAdvisor())
            )
            .build();
}
```

为了和上一个例子做区分，这里创建出的名字叫 **assistantChatClient**

## 创建Controller实现对话功能
```java
@RequestMapping(value = "/chat", produces = "text/html;charset=utf-8")
public Flux<String> chat(@RequestParam("prompt") String prompt) {
    // 请求模型
    return assistantChatClient.prompt()
            .user(prompt)
            .stream()
            .content();
}
```

到这里，就把简单的对话完成了，但是到这里是远远不够的，因为此时的ai并不具备联系上下文的功能。

## 什么是联系上下文？
当你和 AI 聊天时，AI 需要知道：
- 你之前问了什么
- 你现在问的和之前的有没有关联

<VipInline />
