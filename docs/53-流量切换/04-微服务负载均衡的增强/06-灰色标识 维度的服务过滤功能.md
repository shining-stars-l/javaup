---
slug: /link-flow/lb-enhancement/gray-flag-filter
title: "灰色标识 维度的服务过滤功能：灰度标识、灰度路由、目标人群切流、小流量验证、实例标签过滤详解"
sidebar_label: "灰色标识 维度的服务过滤功能"
pagination_label: "灰色标识 维度的服务过滤功能"
description: "灰度标识维度服务过滤机制，基于灰度标签识别目标人群流量，实现新版本小流量验证与稳定性观测。内容进一步围绕灰度路由、目标人群切流、实例标签过滤、发布实验、风险控制等关键主题展开。通过原理拆解、实现步骤与适用场景说明相关方案如何落地。同时补充常见问题、排查思路、项目实践建议与技术面试要点。帮助开发者建立完整知识体系，并将…"
keywords: ["灰度标识", "灰度路由", "目标人群切流", "小流量验证", "实例标签过滤", "稳定性观测", "发布实验", "风险控制"]
---

import VipInline from '@site/src/components/VipInline';

# 灰色标识 维度的服务过滤功能

预生产环境功能是为了让测试时，能够在生产环境中的进行测试，而不影响到生产环境的服务。这样可以让测试更加全面，避免由于测试和生产环境不一致而产生的隐患问题。

关于此部分的详细讲解，请跳转到 技术精华讲解-设计预生产环境让测试更加严谨 章节

而实现在同一个注册中心下，预生产服务和生产服务隔离的办法就是让预生产服务中多一个灰色标识，只要存在这个这个标识，那么这个服务就是预生产的

## 灰色标识的设置
在配置中指定开启或关闭（默认关闭状态）
```yaml
spring:
  application:
    link-flow:
      work:
        # 灰度标识开关
        gray:
          activate: true
```
具体服务自己的灰色标识值通过在项目的配置文件中指定
```yaml
spring:
  cloud:
    link-flow:
      metadata:
        gray: true
```
在项目启动后，就会自动将灰色标识放置在服务中的元数据中
![灰色标识的设置示意图 1](/img/link-flow/灰色标识维度的服务过滤功能/1.png)
![灰色标识的设置：预生产环境](/img/link-flow/灰色标识维度的服务过滤功能/预生产环境.jpg)

<VipInline />
